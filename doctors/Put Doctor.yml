info:
  name: Put Doctor
  type: http
  seq: 1

http:
  method: PUT
  url: http://127.0.0.1:5000/doctors/1
  body:
    type: json
    data: |-
      {
        "name": "Luigi", 
        "medical_speciality" : "Pediatric"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​
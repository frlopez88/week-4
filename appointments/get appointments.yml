info:
  name: get appointments
  type: http

http:
  method: GET
  url: http://127.0.0.1:5000/appointments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
